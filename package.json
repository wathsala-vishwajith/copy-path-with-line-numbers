{
  "name": "copy-path-with-line-numbers",
  "displayName": "Copy Path with Line Numbers",
  "description": "Copy relative/absolute path of a file with line numbers.",
  "version": "0.0.1",
  "publisher": "WathsalaVishwajith",
  "repository": {"type": "git", "url": "https://github.com/wathsala-vishwajith/copy-path-with-line-numbers"},
  "icon": "icons/icon.png",
  "categories": [
    "Other"
  ],
  "keywords": ["copy path", "copy line numbers", "line numbers", "clipboard", "markdown copy"],
  "engines": {
    "vscode": "^1.125.0"
  },
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "copyRelativePathWithLines.copy",
        "title": "Copy Relative Path with Line Number(s)"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "copyRelativePathWithLines.copy",
          "when": "editorTextFocus",
          "group": "9_copypath"
        }
      ]
    },
    "keybindings": [
      {
        "command": "copyRelativePathWithLines.copy",
        "key": "ctrl+alt+c",
        "mac": "cmd+alt+c",
        "when": "editorTextFocus"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "pnpm run package",
    "compile": "pnpm run check-types && pnpm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "pnpm run check-types && pnpm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "pnpm run compile-tests && pnpm run compile && pnpm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.125.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "24.x",
    "typescript-eslint": "^8.61.1",
    "eslint": "^10.5.0",
    "esbuild": "^0.28.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^6.0.3",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0"
  },
  "overrides": {
    "diff": "^8.0.4",
    "serialize-javascript": "^7.0.6"
  }
}
